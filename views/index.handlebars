<header class="center-align valign-wrapper">
    <div class="container ">
        <div class="row">
            <div class="col s12">
                <h1>Philly Neighborhood Bulletin</h1>
            </div>
        </div>
    </div>
</header>

<main>
    <div class="container">
        <div class="row">
            <div class="col s12">
                <h3>Welcome!</h3>
                <p>Philly Neighborhood Bulletin is the central hub for Philadelphia neighbors to find and share information with eachother. From yard sales and farmers markets to safety tips and lost items, select your neighborhood below to see what's happening or create a post.</p>
            </div>
        </div>
        <div class="row">
            
            {{!-- TO DO: offset columns so they are always centered on page --}}
            <div class="col s12">
                <h4>Philly Neighborhoods:</h4>
                <ul>
                    {{#each neighborhoods}}
                    <li><a href="/hoods/{{this.id}}" data-id="{{this.id}}">{{this.name}}</a></li>
                    {{/each}}
                </ul>
                
            </div>
        </div>
    </div>
</main>

<footer class="center-align valign-wrapper">
    <div class="container ">
        <div class="row">
            <div class="col s12">
                <a href="#">GitHub</a>
                <a href="/">Home</a>
                <a href="/newpost/">New Post</a>
            </div>
        </div>
    </div>
</footer>